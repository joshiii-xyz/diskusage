[package]
name = "diskusage"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Show disk usage sorted by size with human-readable output"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
